# Used by `bun run dev`. Keep this non-interactive and avoid launching xTool.
extends = ["flatmaxxing.user.toml"]

[validation.isolationFeasibility]
ignore = ["AE1"]

[board]
file = "new-and-improved-zefir-btn.kicad_pcb"
autoFix = false

[solderMask]
generate = false

[stencil]
generate = false
